#include <stdio.h>
#include <Windows.h>

int main() {
	int fala;
	int direct;
	for(fala = 1, direct=1 ; fala >-1; fala=fala+direct){
		if (fala == 100) {
			direct = -1;
		}
			printf("Fala leci %d \n", fala);
		
		
		

	}
		

	return 0;
}
